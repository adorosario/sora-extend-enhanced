# ============================================================================
# SORA VIDEO EXTENSION - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and fill in your values
# Command: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED VARIABLES
# ----------------------------------------------------------------------------

# OpenAI API Key (REQUIRED)
# Get your key from: https://platform.openai.com/api-keys
# Make sure you have access to Sora 2 API and GPT-5 (or alternative planning model)
OPENAI_API_KEY=sk-proj-your-api-key-here

# Base Prompt (REQUIRED)
# This is the main description of the video you want to create
# The AI will break this down into multiple coherent segments
# Examples:
#   - "Gameplay footage of a game releasing in 2027, a car driving through a futuristic city"
#   - "Intro video for the iPhone 19"
#   - "A cinematic tour through a modern tech office"
BASE_PROMPT="Your video description here"

# ----------------------------------------------------------------------------
# VIDEO GENERATION SETTINGS
# ----------------------------------------------------------------------------

# Seconds Per Segment (REQUIRED)
# How long each individual segment should be
# Options: 4, 8, or 12 seconds
# Recommended: 8 (good balance of quality and speed)
SECONDS_PER_SEGMENT=8

# Number of Generations (REQUIRED)
# How many segments to generate
# Total video length = SECONDS_PER_SEGMENT × NUM_GENERATIONS
# Example: 8 seconds × 5 segments = 40 second video
NUM_GENERATIONS=2

# ----------------------------------------------------------------------------
# MODEL CONFIGURATION (OPTIONAL)
# ----------------------------------------------------------------------------

# Planner Model (OPTIONAL)
# The AI model used to plan and break down your prompt into segments
# Default: gpt-5
# Alternatives: gpt-4o, gpt-4-turbo (if you don't have GPT-5 access)
PLANNER_MODEL=gpt-5

# Sora Model (OPTIONAL)
# The Sora model to use for video generation
# Options: sora-2, sora-2-pro
# Default: sora-2
# Note: sora-2-pro is higher quality but slower and more expensive
SORA_MODEL=sora-2

# Video Size (OPTIONAL)
# Resolution for generated videos
# Must stay constant across all segments
# Default: 1280x720
# Options: 1280x720, 1920x1080, etc.
SIZE=1280x720

# ----------------------------------------------------------------------------
# ADVANCED SETTINGS (OPTIONAL)
# ----------------------------------------------------------------------------

# Polling Interval (OPTIONAL)
# How often to check video generation status (in seconds)
# Default: 2
# Lower = more API calls but faster notification
POLL_INTERVAL_SEC=2

# API Base URL (OPTIONAL)
# OpenAI API endpoint
# Default: https://api.openai.com/v1
# Only change if using a proxy or custom endpoint
API_BASE=https://api.openai.com/v1

# Output Directory (OPTIONAL)
# Where to save generated videos inside the container
# Default: /app/output/sora_ai_planned_chain
# The ./output directory is automatically mounted to your host
OUTPUT_DIR=/app/output/sora_ai_planned_chain
